# Port Render will set its own PORT env; keep this as fallback
PORT=3000

# ICE servers JSON: include STUN/TURN for reliable connectivity
# Example (replace with real credentials):
ICE_SERVERS=[
  {"urls":["stun:stun.l.google.com:19302"]}
]


# Optional: production origin allowed (your admin site)
FRONTEND_ORIGIN=https://anantkripa.in
